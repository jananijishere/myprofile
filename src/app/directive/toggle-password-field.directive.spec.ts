import { TogglePasswordFieldDirective } from './toggle-password-field.directive';

describe('TogglePasswordFieldDirective', () => {
  it('should create an instance', () => {
    const directive = new TogglePasswordFieldDirective();
    expect(directive).toBeTruthy();
  });
});
